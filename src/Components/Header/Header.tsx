import React from 'react';
import './header.sass';

const Header: React.FC = () => {
  return (
    <header className={'header-main'}>
      <span>здесь у нас шапка сайта</span>
    </header>
  );
};

export default Header;