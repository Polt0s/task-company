import React from 'react';
import './footer.sass';

const Footer: React.FC = () => {
  return (
    <footer className={'footer-main'}>
      <span>здесь фотер приложения!</span>
    </footer>
  );
};

export default Footer;